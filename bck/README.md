# BCK：历史代码

这里存放已经不属于当前 MVP 主链路的历史实现。当前 MVP 的代码、部署脚本和验收入口统一位于 `../mvp/`。

## 目录

- `legacy-prototype/`：上一版 DSH + Multica Runtime 原型。

该目录仅用于追溯设计和比较实现，不作为当前部署入口。旧原型的 `package.json` 仍在 `legacy-prototype/` 内；如需运行旧测试或重新生成插件 bundle，应在该目录下执行命令。

