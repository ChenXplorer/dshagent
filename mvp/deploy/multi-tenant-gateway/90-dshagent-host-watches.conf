# DSH Web Hosts use filesystem watchers for Profile and Skill reloads.
# Keep headroom for many on-demand tenant Hosts under one service account.
fs.inotify.max_user_instances = 1024
fs.inotify.max_user_watches = 524288
fs.inotify.max_queued_events = 32768
